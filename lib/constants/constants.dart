import 'package:flutter/material.dart';

const kTaskScreenTextStyle =
    TextStyle(fontSize: 50, color: Colors.white, fontWeight: FontWeight.w900);

const kAddTaskStyle = TextStyle(
    fontSize: 30, color: Colors.lightBlue, fontWeight: FontWeight.w600);

const kDecoration = BoxDecoration(
  borderRadius: BorderRadius.only(
      topLeft: Radius.circular(20), topRight: Radius.circular(20)),
  color: Colors.white,
);
